{
  "name": "nef",
  "version": "1.0.0",
  "description": "Generate JSON metadata for writing entries in a directory.",
  "bin": {
    "nef": "./nef.js"
  },
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
